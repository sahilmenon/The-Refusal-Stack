source_behaviors: advbench  # AdvBench harmful prompts -> canned refusals, plus
                            # the curated refusal demos load_reharden injects.
n_harmful: 200
n_held_out: 80
seed: 42
split_ratio: 0.7
chat_template: llama3
output_dir: data/finetune/
