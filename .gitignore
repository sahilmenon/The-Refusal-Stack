# Local planning notes and assistant tooling — keep out of the repo
plan.md
.claude/
CLAUDE.md
.cursor/
.aider*
.playwright-mcp/

# Secrets
.env
.env.*
!.env.example
.secrets/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.ipynb_checkpoints/

# Experiment tracking and ML artifacts
wandb/
outputs/
checkpoints/
.cache/
artifacts/*.safetensors
# Fine-tune adapter dirs are tampered/control weights (adapter_model.safetensors
# lives one level down, so the rule above misses it) — never track, per the
# content-warning policy that tampered weights stay out of version control.
artifacts/*_lora/
artifacts/llama2/
data/finetune/

# Generated datasets (regenerated by `make data`) and raw dataset caches
data/refusal_eval_dataset.jsonl
data/advbench/
data/harmbench/
data/alpaca/

# OS
.DS_Store
Thumbs.db

# Reference paper PDFs (analysis only, not shipped)
papers/

# Generated run artifacts — must NOT be tracked (git archive shipped empty copies
# to pods, whose live sync then clobbered real local results).
/results/*.json
!/results/phase2_attacks_llama_corrected.json
/logs/*.log
/logs/*.json
/figures/*.png
/figures/**/*.png
/figures/figures/
/vlm_modality_gap.json

# Phase-3 -> Phase-4 handoff: keep the canonical refusal direction tracked
!/artifacts/refusal_direction_latest.safetensors
